# name: iservice
# key: iservice
# --
public class ${1:`(s-upper-camel-case (file-name-nondirectory (file-name-sans-extension (buffer-file-name))))`} extends Service {
    @Override    
    public IBinder onBind(Intent intent) {
	return null;
    }

    @Override
    public int onStartCommand(Intent intent, int flag, int startId) {
	$0
	return START_NOT_STICKY;
    }
}
